# План: регистрация бизнеса в ONLAIS

Статус: `v4`  
Дата обновления: `2026-04-19`

## 1. Цель

Сделать корректную самостоятельную регистрацию нового бизнеса в CRM ONLAIS:
- вход по `email + пароль` (без ручного `accountSlug`),
- выбор организационно-правовой формы и направления деятельности,
- обязательные юридические согласия при регистрации,
- автоматическое создание аккаунта и сайта-черновика,
- жесткая уникальность owner-контактов (email/телефон).

## 2. Фиксированные бизнес-правила

- Один владелец бизнеса = один уникальный `email` и один уникальный `телефон`.
- Нельзя зарегистрировать новый аккаунт, если `email` уже используется в другом аккаунте владельца.
- Нельзя зарегистрировать новый аккаунт, если `телефон` уже используется в другом аккаунте владельца.
- Проверка уникальности делается до финального создания аккаунта и дублируется ограничениями БД.
- Публичная ссылка (`slug`) создается автоматически; в форме регистрации пользователь ее не вводит.
- Авторизация в CRM: только `email + пароль`; выбор аккаунта появляется только если у пользователя несколько аккаунтов.

## 3. Целевой UX регистрации

1. Шаг 1: `Email + пароль`.
2. Шаг 2: подтверждение email (код/ссылка).
3. Шаг 3: данные бизнеса:
   - название,
   - форма: `Частный специалист / Самозанятый / ИП / ООО`,
   - направление деятельности (из каталога ONLAIS),
   - телефон,
   - часовой пояс.
4. Шаг 4: обязательные согласия (чекбоксы).
5. Шаг 5: создание аккаунта, автологин, создание стартового сайта.

## 4. Изменения в БД (Prisma)

### 4.1 Новые/уточненные сущности

- [x] Enum `LegalType`:
  - [x] `PRIVATE_SPECIALIST`
  - [x] `SELF_EMPLOYED`
  - [x] `IP`
  - [x] `OOO`

- [x] Enum `BusinessType` (источник: `docs/SERVICE_CATALOG_MASTER.md`, текущая версия каталога: `v3`).

- [x] В `Account`:
  - [x] `legalType LegalType?`
  - [x] `businessType BusinessType?`
  - [x] `onboardingCompletedAt DateTime?`

- [x] Таблица подтверждения email (`EmailVerificationToken` или эквивалент).
- [x] Таблица фиксации согласий (`LegalAcceptance`) используется в сценарии регистрации.

### 4.2 Ограничения уникальности

- [x] Уникальный индекс для owner email.
- [x] Уникальный индекс для owner phone.
- [x] Явно описать правило, какой пользователь считается владельцем (`OWNER`).

## 5. Backend API

### 5.1 Новые endpoints

- [x] `POST /api/v1/crm/auth/register/start`
  - вход: `email`, `password`
  - результат: отправка кода/ссылки подтверждения

- [x] `POST /api/v1/crm/auth/register/verify-email`
  - вход: токен/код
  - результат: email подтвержден

- [x] `POST /api/v1/crm/auth/register/complete`
  - вход: бизнес-данные + согласия
  - транзакция:
    - [x] создать `User`/identity,
    - [x] создать `Account` и `slug`,
    - [x] назначить роль `OWNER`,
    - [x] создать сайт-черновик,
    - [x] зафиксировать согласия,
    - [x] создать сессию.

- [x] `POST /api/v1/crm/auth/select-account` (для мультиаккаунта).

### 5.2 Обновление логина

- [x] `POST /api/v1/crm/auth/login` без `accountSlug`.
- [x] Если аккаунтов > 1, вернуть список аккаунтов для выбора.

## 6. Frontend (CRM)

- [x] Новая страница `/crm/register` (wizard).
- [x] Обновить `/crm/login`:
  - [x] убрать поле `accountSlug`,
  - [x] добавить шаг выбора аккаунта (если их несколько).
- [x] Ошибки валидации:
  - [x] email уже занят,
  - [x] телефон уже занят,
  - [x] слабый пароль,
  - [x] не приняты обязательные документы.

## 7. Юридический пакет (обязательно)

- [x] Пользовательское соглашение (оферта)
- [x] Политика конфиденциальности
- [x] Согласие на обработку персональных данных
- [x] Поручение на обработку ПДн (DPA) для клиентов CRM
- [x] Политика cookies
- [x] Согласие на рекламно-информационные рассылки (опционально)

Фиксация согласий в БД:
- [x] `documentKey` (через связь `LegalAcceptance -> LegalDocumentVersion -> LegalDocument`)
- [x] `documentVersion` (через `documentVersionId`)
- [x] `acceptedAt`
- [x] `ip`
- [x] `userAgent`
- [x] `accountId` (`userId` как отдельное поле отсутствует в текущей модели)

## 8. Каталог направлений ONLAIS (интеграция в план)

Источник: [SERVICE_CATALOG_MASTER.md](C:\Users\Nadin\BeautyPlatform\docs\SERVICE_CATALOG_MASTER.md)  
Текущая версия каталога: `v3 (на согласование)`

### 8.1 Правило использования каталога

- [x] Каталог направлений из `SERVICE_CATALOG_MASTER.md` является источником для поля регистрации «Направление деятельности».
- [x] В UI регистрации показывать русское название направления.
- [x] В БД сохранять только `key` (`BusinessType`).
- [x] Список направлений включает только сценарии, подходящие для ONLAIS: услуги + слоты + специалисты + групповые занятия.

### 8.2 Обязательные внедрения по каталогу

- [x] Синхронизировать `BusinessType enum` с актуальным списком в `SERVICE_CATALOG_MASTER.md`.
- [x] Подготовить миграцию при изменении enum.
- [x] Добавить серверную валидацию: принимаются только значения из каталога.
- [x] Добавить фронтовую валидацию и сортировку списка направлений.

### 8.3 Онбординг на базе каталога

- [x] Каталог НЕ создает автоматически услуги/категории/слоты в CRM: это настраивает владелец бизнеса вручную.
- [x] При создании сайта-черновика подставлять дефолтные тексты блоков в конструкторе по выбранному `BusinessType`.

### 8.4 Изменения каталога

- [x] Любые изменения в `SERVICE_CATALOG_MASTER.md` фиксировать с версией (`vX`) и датой.
- [x] При изменении ключей/структуры каталога проверять обратную совместимость с существующими аккаунтами (скрипт `check:business-catalog-compat`).

## 9. Админка (второй этап, после self-service)

- [x] Перевести регистрацию через админку в сценарий приглашения:
  - [x] админ создает черновик,
  - [x] пользователь завершает регистрацию сам,
  - [x] согласия принимает только пользователь.
- [x] Статусы: `DRAFT`, `INVITED`, `ACTIVE`.

## 10. Безопасность и контроль

- [x] rate-limit: старт регистрации, подтверждение email, логин
- [x] лимиты попыток OTP и TTL
- [x] аудит регистрации и согласий
- [x] анти-бот базовый (captcha по порогу риска)

## 11. Тесты

- [ ] успешная регистрация
- [ ] дубль email
- [ ] дубль телефона
- [ ] отсутствие обязательных согласий
- [ ] истечение токена подтверждения
- [ ] логин пользователя с несколькими аккаунтами
- [ ] выбор направления только из каталога `SERVICE_CATALOG_MASTER.md`
- [ ] корректная автоподстановка текстовых шаблонов конструктора сайта по направлению

## 12. Порядок внедрения

1. Prisma-схема + миграции
2. Синхронизация `BusinessType` с `SERVICE_CATALOG_MASTER.md`
3. API регистрации
4. UI регистрации
5. Логин без `accountSlug`
6. Юридический пакет и фиксация согласий
7. Подстановка текстового контента конструктора сайта по направлению
8. Админ-приглашения (этап 2)

## 13. Текущий прогресс

- [x] Сформирован целевой план регистрации
- [x] Определены бизнес-правила уникальности email/телефона
- [x] Подготовлен каталог направлений ONLAIS для записи по слотам (`SERVICE_CATALOG_MASTER.md`)
- [x] Подготовлен юридический пакет
- [x] Реализованы базовые API и UI self-service регистрации (v1)
- [x] Добавлен базовый анти-бот, аудит регистрации и подстановка текстов сайта по `BusinessType`
- [x] Реализован admin invite-flow (`DRAFT` → `INVITED` → `ACTIVE`) без автосоздания услуг/слотов
